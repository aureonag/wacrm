// ============================================================
// Contrato: Aureon's own (CONTRATADA) identity shown on the public
// signing page header. Fixed agency-wide data — confirmed by the
// user directly in the contract text they provided (see the
// "CONTRATADA" block of the seeded template), not per-account or
// per-deal, so it lives as a constant rather than a DB column.
// ============================================================

export const AUREON_PARTY = {
  name: "Aureon Publicidade LTDA",
  cnpj: "54.083.676/0001-15",
  representative: "Allan Victor Silva Giro",
  email: "allan.giro@aureonag.com.br",
};
